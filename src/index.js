
/**
 * Gets the processing page
 * @param {array} data
 */
function getProcessingPage(data = []) {
  return new Promise((resolve, reject) => {
    resolve();
  });
}


module.exports = {
  getProcessingPage,
};
